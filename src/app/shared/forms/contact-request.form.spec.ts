import { ContactRequestForm } from './contact-request.form';

describe('ContactRequestForm', () => {
  it('should create an instance', () => {
    expect(new ContactRequestForm()).toBeTruthy();
  });
});
